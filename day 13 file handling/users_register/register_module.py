import os
import csv

with open('file.csv', newline='') as f:
    reader = csv6+6.reader(f)
    data = list(reader)

print(data)
def register(id,username,password):
    username=input("Enter username:")
    password=input("Enter password:")
    open("record.txt","r+")